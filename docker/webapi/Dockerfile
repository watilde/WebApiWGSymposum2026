# OHDSI WebAPI — Hackathon Sandbox
# Based on the official OHDSI WebAPI image.
# https://github.com/OHDSI/WebAPI
FROM ohdsi/webapi:latest

# The base image already exposes 8080 and sets up the WAR.
# Environment variables (DATASOURCE_URL, etc.) are injected via docker-compose.
EXPOSE 8080
